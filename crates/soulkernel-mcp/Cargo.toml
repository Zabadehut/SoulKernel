[package]
name = "soulkernel-mcp"
version = "1.1.9"
edition = "2021"
description = "SoulKernel MCP server for live report metrics"

[[bin]]
name = "soulkernel-mcp"
path = "src/main.rs"

[dependencies]
flate2 = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
